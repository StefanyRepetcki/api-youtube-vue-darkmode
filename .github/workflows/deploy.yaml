name: deploy
on:
  push:
    branches:
      - master
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
        with:
          node-version: '12'
      - name: setup git auth
        env:
          email: 'stefany.c.r@hotmail.com'
          username: 'StefanyRepetcki'
          access_token: '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'
        run: |
          git config --global credential.helper store
          echo "https://${access_token}:x-oauth-basic@github.com" > ~/.git-credentials
          git config --global user.email stefany.c.r@hotmail.com
          git config --global user.name StefanyRepetcki
      - name: deploy to gh-pages
        run: |
          yarn install
          NODE_ENV=production yarn run build
          cd dist
          git init
          git add -A
          git commit -m "🚀 Deploy id ${GITHUB_RUN_ID}"
          git push -f "https://github.com/${GITHUB_REPOSITORY}.git" master:gh-pages